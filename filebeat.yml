filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - /zeek_logs/*.log
      - /suricata_logs/eve.json

output.elasticsearch:
  hosts: ["http://elasticsearch:9200"]

setup.kibana:
  host: "http://kibana:5601"
